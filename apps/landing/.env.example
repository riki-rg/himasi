# API backend (tanpa trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8010/api/v1

# App komunitas — kosongkan bila belum deploy (CTA fallback ke '#')
NEXT_PUBLIC_BITSI_URL=
NEXT_PUBLIC_SIBINER_URL=
